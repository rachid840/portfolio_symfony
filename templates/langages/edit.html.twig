{% extends 'base.html.twig' %}

{% block title %}Edit Langages{% endblock %}

{% block body %}
    <h1>Edit Langages</h1>

    {{ include('langages/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('langages_index') }}">back to list</a>

    {{ include('langages/_delete_form.html.twig') }}
{% endblock %}
