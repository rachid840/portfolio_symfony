{% extends 'base.html.twig' %}

{% block title %}Langages{% endblock %}

{% block body %}
    <h1>Langages</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ langage.id }}</td>
            </tr>
            <tr>
                <th>Titre</th>
                <td>{{ langage.titre }}</td>
            </tr>
            <tr>
                <th>Experience</th>
                <td>{{ langage.experience }}</td>
            </tr>
            <tr>
                <th>Logo</th>
                <td>{{ langage.logo }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('langages_index') }}">back to list</a>

    <a href="{{ path('langages_edit', {'id': langage.id}) }}">edit</a>

    {{ include('langages/_delete_form.html.twig') }}
{% endblock %}
